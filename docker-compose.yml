version: '3.8'

services:
  q_immune_qds:
    build: .
    container_name: q_immune_qds_app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
